Realization of voxel coloring algorithm on C++ via OpenCL.

Using openclxx library. More info search in src/CLxx/README file.

Dependencies:
    opencl

covc:
    library.

dependencies/ocl:
    Experimental C++ Bindings.

dependencies/matrix:
    matrix lib.

example/qcovc:
    GUI application shows how to use covc.

tools/oclc:
  oclc - OpenCL Compiler.
  oclc only compiles OpenCL sources and showing errors.

